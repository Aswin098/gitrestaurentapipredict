from django.contrib import admin
from .models import Restaurant  # Import the Restaurant model

# Removed registration of the Restaurant model with the admin interface
# admin.site.register(Restaurant)
